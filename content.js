// Injected on demand into the active tab at the moment of invocation, never at
// page load. Reads the DOM, draws the region-selection overlay, highlights
// source phrases. Never calls the model API and never touches chrome.storage.
// See PRD 9.3.

// MVP 0 scaffold: extraction is added in MVP 2 and MVP 4.
