// Placeholder so the suite runs green on the empty scaffold. MVP 0 requires no
// unit tests; real tests arrive with MVP 1. Replace this file then.
import { describe, it, expect } from "vitest";

describe("scaffold", () => {
  it("runs the test suite", () => {
    expect(true).toBe(true);
  });
});
