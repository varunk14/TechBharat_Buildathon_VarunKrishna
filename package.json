{
  "name": "lens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for in-page summarization",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0"
  }
}
